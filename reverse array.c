#include <stdio.h>
int main()
{
    int n,i;
    printf("array size");
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(i=n-1;i>=0;i--)
    {
        int k=0;
        if( int k=n*10+n[i])
        {
          scanf("%d",arr[i]);
        }
        else
        {
          printf("%d",k)
        }

    }


}
