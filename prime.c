#include <stdio.h>
#include <math.h>
int main()
{
    int i,n,isprime;
    printf("entre num");
    scanf("%d",&n);
    for(i=2;i<=sqrt[n];i++)
    {
        if(n%i==0)
        is prime=0;
        break;
    }
    if(isprime==1)
    {
        printf("prime");
        else
            printf("not a prime");
    }

}
